# Luminfield 项目协作规则

本文件适用于仓库根目录及全部子目录。用户当前明确要求与更高层级规则优先。

## 项目边界

- 使用 Godot 4.7.1 .NET、C# 和 `net8.0`；SDK 版本由 `global.json` 固定。
- 保持世界观、角色、剧情、文本、代码、美术和音频原创，不复制或反编译《星露谷物语》等商业游戏内容。
- `GameSession` 是唯一全局业务入口；领域规则放在 `src/Core/`，场景和 HUD 只展示状态、转发输入并订阅事件。
- 稳定物品、作物、资源和交互类型使用 ID；存档只保存稳定 ID，不保存翻译文本或场景节点引用。

## 目标区与交互提示

- 所有面向目标格执行的行为都必须提供交互预览，不能只实现按键后的结果。
- 预览必须复用 `GameSession.PreviewSelectedTarget` 和 `TargetPreview`，与实际动作读取同一份工具、目标、体力、容量和状态规则；预览函数严禁修改业务状态。
- 可执行目标使用青绿色高亮并显示 `E · 动作`；工具不匹配使用暖金色并显示所需工具；体力、水量、种子或背包容量不足使用玫红色并说明原因；普通不可交互格只保留弱化目标框。
- 高亮必须覆盖实际目标物轮廓，而不只是脚下格：树、晶体、水面、作物、人物、门、设施和床应使用对应的 `TargetPreviewKind`。
- 邻接即可触发的对象必须高亮真实对象位置，并与输入判定保持一致，不能高亮玩家朝向的无关格。
- 后续新增砍伐、采矿、钓鱼、拾取、开箱、商店、制作或任务设施时，必须同时补齐：
  1. 只读预览规则；
  2. 实际动作规则；
  3. 对象轮廓或范围高亮；
  4. 简体中文与英文动作/失败文本；
  5. 可执行、工具不匹配和资源不足测试。
- 无效操作不得扣除体力、水量、物品或货币，也不得提前改变地图、任务或存档状态。

## 工具、背包与农业

- 快捷栏共 8 格，背包共 24 格；前五格固定为手、铲子、柴刀、浇水壶和水桶。
- 手承担交谈、设施交互、查看和成熟作物收获；铲子承担开垦与挖水晶；柴刀承担砍树；水桶只在真实水源补充浇水壶。
- 新物品优先复用现有堆叠、容量检查和稳定 ID；不得绕过 `Inventory` 直接修改槽位。
- 农田、作物、世界资源和任务状态必须由领域模型驱动；背景图和生成图集不得承载业务状态。

## 美术与本地化

- 保持 640×360 内部分辨率、最近邻过滤、16×16 基础瓦片和暮色靛蓝、发光青绿、暖金色的“Luminfield 魔法田野”风格。
- 缺少位图素材时使用内置图像生成制作原创图集；运行时裁切使用稳定 ID，并在 `assets/generated/README.md` 记录用途和提示词摘要。
- UI、目标提示、对话和失败原因全部使用本地化键；`zh_CN.json` 与 `en.json` 必须拥有完全相同的键集合。

## 存档与兼容

- 存档继续通过 Godot `user://` 路径和 `System.Text.Json` 原子写入。
- 新增可持久化状态时优先做向后兼容的加法式迁移；未知版本和损坏存档仍按现有恢复策略处理。
- 读取旧存档时保留已有日期、体力、背包、作物、任务、探索和资源状态，不静默清档。

## 修改与验证

- 修改前确认仓库路径、分支、HEAD 和工作区状态；现有未提交内容视为用户工作，不回退、不覆盖、不顺手整理无关文件。
- 代码修改使用小而完整的增量，不为一次交互增加平行业务状态或场景节点互相查找。
- 每次相关改动至少执行 C# 编译、核心测试、Godot 无界面导入/启动和 `git diff --check`。
- 目标高亮、生成美术、像素缩放或布局变化必须在当前 macOS 上实际查看；Windows 与 Linux 只在对应系统运行后才能声称实机通过。
- 未经用户明确授权，不提交、推送、发布、清理工作区或重新导出分享包。
- 仓库映射到 `/Users/edy/Documents/Obsidian Personal/Luminfield`；验证通过后按项目工作流更新模块说明和当天变更记录，不写入 `Obsidian Work`。

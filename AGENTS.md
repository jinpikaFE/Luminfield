# Luminfield 项目协作规则

本文件适用于仓库根目录及全部子目录。用户当前明确要求与更高层级规则优先。

## 项目边界

- 使用 Godot 4.7.1 .NET、C# 和 `net8.0`；SDK 版本由 `global.json` 固定。
- 保持世界观、角色、剧情、文本、代码、美术和音频原创，不复制或反编译《星露谷物语》等商业游戏内容。
- `GameSession` 是唯一全局业务入口；领域规则放在 `src/Core/`，场景和 HUD 只展示状态、转发输入并订阅事件。
- 稳定物品、作物、资源和交互类型使用 ID；存档只保存稳定 ID，不保存翻译文本或场景节点引用。

## 目标区与交互提示

- 所有面向目标格执行的行为都必须提供交互预览，不能只实现按键后的结果。
- 预览必须复用 `GameSession.PreviewSelectedTarget` 和 `TargetPreview`，与实际动作读取同一份工具、目标、体力、容量和状态规则；预览函数严禁修改业务状态。
- 可执行目标使用青绿色高亮并显示 `E · 动作`；工具不匹配使用暖金色并显示所需工具；体力、水量、种子或背包容量不足使用玫红色并说明原因；普通不可交互格只保留弱化目标框。
- 高亮必须覆盖实际目标物轮廓，而不只是脚下格：树、晶体、水面、作物、人物、门、设施和床应使用对应的 `TargetPreviewKind`。
- 邻接即可触发的对象必须高亮真实对象位置，并与输入判定保持一致，不能高亮玩家朝向的无关格。
- 后续新增砍伐、采矿、钓鱼、拾取、开箱、商店、制作或任务设施时，必须同时补齐：
  1. 只读预览规则；
  2. 实际动作规则；
  3. 对象轮廓或范围高亮；
  4. 简体中文与英文动作/失败文本；
  5. 可执行、工具不匹配和资源不足测试。
- 无效操作不得扣除体力、水量、物品或货币，也不得提前改变地图、任务或存档状态。

## 工具、背包与农业

- 快捷栏共 8 格，背包共 24 格；前五格固定为手、铲子、柴刀、浇水壶和水桶。
- 手承担交谈、设施交互、查看和成熟作物收获；铲子承担开垦与挖水晶；柴刀承担砍树；水桶只在真实水源补充浇水壶。
- 新物品优先复用现有堆叠、容量检查和稳定 ID；不得绕过 `Inventory` 直接修改槽位。
- 农田、作物、世界资源和任务状态必须由领域模型驱动；背景图和生成图集不得承载业务状态。

## 美术与本地化

- 保持 640×360 内部分辨率、最近邻过滤、16×16 基础瓦片和暮色靛蓝、发光青绿、暖金色的“Luminfield 魔法田野”风格。
- 新增具有地图实体、角色、作物、物品、设施、天气表现或独立 UI 图标的玩法前，必须先盘点现有图集；缺少合适位图时，先使用内置图像生成制作原创素材，再实现最终场景表现，不得把临时色块、几何图形或字符图标当作完成态。
- 生图前先写清资产用途、单元尺寸、图集排列、透明或色键背景、观察角度、光照、调色板和禁止项；生成结果必须与现有暮色魔法农场保持一致，不能只在提示词中写“像素风”。
- 项目内素材使用内置图像生成的默认流程；需要透明背景时先生成纯色色键底图，再使用本地去色键脚本得到透明 PNG。生成源图、最终运行时图与对应 `.png.import` 必须按运行时职责保存到 `assets/generated/<分类>/<子域>/` 的同一最末级目录，不得只保留在临时目录，也不得把 PNG 堆放在 `assets/generated/` 根目录。
- 生成资源按职责分类：角色放 `characters/`，动物放 `animals/`，农业放 `farming/`，战斗/节庆/钓鱼/采矿放 `activities/`，委托/施工/邮件/加工/关系/星灯/储物等功能点放 `features/`，地点放 `locations/`，跨场景图标放 `ui/`，家园/探索/天气放 `world/`；当前运行时不加载的历史资源才可进入 `legacy/`。新增子域前先更新 `assets/README.md` 与 `assets/generated/README.md`。
- 代码和场景只引用明确指定的运行时资源；需要以色键原图作为运行时材质输入时，必须在生成素材说明中明确记录。`legacy/` 不得存在运行时引用；恢复历史资源前必须先迁入对应职责目录并重新验收。
- 移动生成资源必须同步更新全部 `res://` 引用、`.png.import` 的 `source_file` 与路径哈希、生成素材说明和相关维护文档，并执行旧路径扫描、C# 编译、核心测试、Godot 导入/启动与 `git diff --check`。
- 生成素材接入前必须实际检查裁切边界、透明通道、像素密度、主体落地基线、红色色键残留和最近邻缩放效果；不合格时先修图或重新生成，不能用运行时代码掩盖明显素材问题。
- 运行时裁切和图集索引使用稳定 ID；生成图只承担视觉表现，碰撞、成熟、天气、设施状态和任务进度仍由领域模型驱动。
- 每次新增或替换生成素材，都在 `assets/generated/README.md` 记录文件用途、图集布局、提示词摘要、色键处理和接入位置。
- UI、目标提示、对话和失败原因全部使用本地化键；`zh_CN.json` 与 `en.json` 必须拥有完全相同的键集合。

## 存档与兼容

- 存档继续通过 Godot `user://` 路径和 `System.Text.Json` 原子写入。
- 新增可持久化状态时优先做向后兼容的加法式迁移；未知版本和损坏存档仍按现有恢复策略处理。
- 读取旧存档时保留已有日期、体力、背包、作物、任务、探索和资源状态，不静默清档。

## 修改与验证

- 修改前确认仓库路径、分支、HEAD 和工作区状态；现有未提交内容视为用户工作，不回退、不覆盖、不顺手整理无关文件。
- 代码修改使用小而完整的增量，不为一次交互增加平行业务状态或场景节点互相查找。
- 验证按改动影响分层，默认执行能够证明本次改动的最小充分集合，不因仓库规模或历史阶段数量机械执行全量回归：
  1. 纯文档、规则或 Obsidian 同步只做内容核对与 `git diff --check`，不运行 C#、Godot 或全量测试；
  2. 局部 C# 逻辑先执行 C# 编译、对应测试类/命名空间的聚焦测试与 `git diff --check`；
  3. 场景、资源或 UI 改动按需追加 Godot 导入/启动、相关确定性试玩入口和当前 macOS 目检，不牵连无关领域测试；
  4. 本地化改动只追加中英键集合检查与受影响界面验收；资源迁移只追加旧路径、导入描述和运行时引用检查。
- 只有以下情况才执行全量回归：形成准备交付、合并、推送或发布的稳定候选；修改存档 schema/迁移、`GameSession` 全局契约、项目依赖/工具链或跨多个领域的共享基础设施；聚焦验证暴露疑似跨模块回归；或用户明确要求。高风险改动也应先完成聚焦验证，待候选稳定后集中执行一次全量门禁，不在开发循环中反复执行。
- 同一源码、测试、项目配置和运行时资源状态已经通过全量门禁后，只有这些范围再次变化才使结果失效；随后仅修改 README、规则或 Obsidian 不得重复触发全量回归。聚焦测试失败时先定位并重跑受影响集合，不能把全量回归当作默认排错手段。
- 当前阶段开发优先使用 `scripts/verify_phase_g_fast.sh`；`scripts/verify_release_candidate.sh` 仅用于上述最终候选门禁。
- 目标高亮、生成美术、像素缩放或布局变化必须在当前 macOS 上实际查看；Windows 与 Linux 只在对应系统运行后才能声称实机通过。
- 未经用户明确授权，不提交、推送、发布、清理工作区或重新导出分享包。
- 仓库通过 Git remote 映射到独立 Obsidian Vault `Obsidian Luminfield` 内的 `Luminfield/` 项目 Hub；验证通过后按项目工作流更新模块说明和当天变更记录，不写入其他个人或工作 Vault。
